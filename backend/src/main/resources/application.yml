server:
  port: 8080
  error:
    include-stacktrace: never
    include-message: never

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vocabapp
    username: postgres
    password: atfbt1771
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  security:
    user:
      name: admin
      password: admin

jwt:
  secret: change-this-to-a-very-long-random-string-at-least-256-bits-for-hs256
  expiration: 86400000